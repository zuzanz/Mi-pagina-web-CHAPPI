<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Eliminar</title>
</head>
<body>
    <h2 class="elimina">Eliminar Producto</h2>
    <style>
        .elimina{
            color: brown;
        }
    </style>

<ul class="eliminar-lista">
    <% productos.forEach(producto => { %>
        <li class="eliminar-card">
            <img src="<%= producto.imagen_url %>" alt="<%= producto.nombre %>" width="100">
            <h3><%= producto.nombre %></h3>
            <p><%= producto.descripcion %></p>
            <strong>Precio: $<%= producto.precio %> CLP</strong>

            <form action="/eliminar-producto/<%= producto.id %>" method="POST">
                <button type="submit" class="btn-eliminar">Eliminar</button>
            </form>
        </li>
    <% }) %>
</ul>
<style>
    .eliminar-lista {
    list-style: none;
    padding: 0;
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 20px;
}

.eliminar-card {
    width: 250px;
    background: rgba(0, 0, 0, 0.7);
    padding: 15px;
    border-radius: 10px;
    box-shadow: 0px 0px 10px red;
    color: white;
    text-align: center;
}

.eliminar-card img {
    border-radius: 5px;
}

.btn-eliminar {
    width: 100%;
    padding: 10px;
    background: red;
    border: none;
    border-radius: 5px;
    color: white;
    font-weight: bold;
    cursor: pointer;
    transition: 0.3s ease-in-out;
}

.btn-eliminar:hover {
    background: darkred;
    box-shadow: 0px 0px 10px red;
}
</style>
<style>
    body {
  background-image: url('/img/robot.jpg'); /* Ajusta la ruta según tu imagen */
  background-size: cover;
  background-position: center;
  background-repeat: no-repeat;
  min-height: 100vh;
  margin: 0;
  font-family: 'Segoe UI', sans-serif;
  color: #fff;
}

/* Contenedor con fondo semitrans
</style>
</body>
</html>