<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registro</title>
</head>
<body>
    <div class="registro-container animado">
  <h1>Crear una cuenta</h1>
  <form action="/registro" method="POST">
    <div class="form-group">
      <input type="text" name="nombre" placeholder="Nombre completo" required>
    </div>
    <div class="form-group">
      <input type="text" name="usuario" placeholder="Nombre de usuario" required>
    </div>
    <div class="form-group">
      <input type="password" name="clave" placeholder="Contraseña" required>
    </div>
    <div class="form-group">
      <input type="email" name="correo" placeholder="Correo electrónico">
    </div>
    <div class="form-group">
      <input type="text" name="telefono" placeholder="Teléfono">
    </div>
    <button type="submit">Registrarme</button>
  </form>
</div>
<style>
    /* Animación de entrada */
@keyframes fadeInUp {
  0% {
    opacity: 0;
    transform: translateY(40px);
  }
  100% {
    opacity: 1;
    transform: translateY(0);
  }
}

.animado {
  animation: fadeInUp 0.8s ease-out;
}
   body {
  background-image: url('/img/juegos.jpg'); /* Cambia el nombre si usás otro */
  background-size: cover;
  background-position: center;
  background-repeat: no-repeat;
  min-height: 100vh;
  margin: 0;
  font-family: 'Segoe UI', sans-serif;
  color: #fff;
}

/* Contenedor del formulario */
.registro-container {
  background-color: rgba(20, 20, 40, 0.9);
  max-width: 500px;
  margin: 80px auto;
  padding: 30px;
  border-radius: 12px;
  box-shadow: 0 0 15px rgba(0, 0, 0, 0.6);
  text-align: center;
}

.registro-container h1 {
  margin-bottom: 25px;
  color: #70ffdf;
}

.form-group {
  margin-bottom: 18px;
}

.form-group input {
  width: 100%;
  padding: 12px 15px;
  border-radius: 8px;
  border: 1px solid #444;
  background-color: rgba(40, 40, 60, 0.95);
  color: #fff;
  font-size: 15px;
  transition: all 0.3s ease;
  box-shadow: inset 0 0 0 transparent;
}

.form-group input::placeholder {
  color: #aaa;
}

.form-group input:focus {
  outline: none;
  border-color: #70ffdf;
  background-color: rgba(55, 55, 75, 1);
  box-shadow: 0 0 6px rgba(112, 255, 223, 0.3);
}

button {
  width: 100%;
  padding: 12px;
  background-color: #5f44f5;
  color: white;
  font-size: 16px;
  border-radius: 8px;
  border: none;
  cursor: pointer;
  transition: background 0.3s ease;
}

button:hover {
  background-color: #4332cc;
} 
</style>

</body>
</html>