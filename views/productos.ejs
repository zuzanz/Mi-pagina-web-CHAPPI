<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Productos</title>
</head>
<body>
    <h1 class="rara">CHAPPI</h1>
    <style>
        .rara{
            color: aliceblue;
        }
    </style>
   <h2 class="duda">Lista de productos</h2>
   <style>
    .duda{
        color: aliceblue;
    }
   </style>
   <ul>
   <h2>Lista de Productos</h2>
<div class="productos-container">
   <% productos.forEach(producto => { %>
    <div class="producto-card">
        <img src="<%= producto.imagen_url %>" alt="<%= producto.nombre %>" width="200">
        <h3><%= producto.nombre %></h3>
        <p><%= producto.descripcion %></p>
        <strong>Precio: $<%= new Intl.NumberFormat('es-CL').format(producto.precio) %> CLP</strong>
        <form action="/borrar-producto/<%= producto.id %>" method="POST">
            <button type="submit">Eliminar</button>
        </form>
    </div>
<% }) %>
</div>
   </ul>
<style>
    .productos-container {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 15px;
    background-color: #1a1a1a; /* Fondo oscuro */
    padding: 20px;
}

.producto-card {
    background-color: #2c2a2a; /* Color de cada producto */
    border: 2px solid #444;
    border-radius: 10px;
    padding: 15px;
    text-align: center;
    width: 250px;
    box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.5);
}

.producto-card h3 {
    color: #ffcc00; /* Amarillo */
}

.producto-card p {
    color: #ffffff; /* Texto blanco */
}

.producto-card strong {
    color: #4CAF50; /* Verde */
}

.producto-card img {
    border-radius: 5px;
}

</style>
   

    <style>
   body {
    background-image: url('/img/goku.jpg');
    background-size: cover;
    background-position: center center;
    background-repeat: no-repeat;
    background-attachment: fixed; /* Mantiene el fondo fijo al hacer scroll */
    font-family: 'Roboto', sans-serif;
}

</style>
</body>
</html>